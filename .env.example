# ===================================
# Market Moodboard Agent Configuration
# ===================================
# Required: PRIVATE_KEY and JATEVO_API_KEY
# Everything else has sensible defaults

# ===== Server Configuration =====
# Port that Bun will listen on locally
PORT=8787
# Base URL surfaced in the agent manifest for absolute links
API_BASE_URL=http://localhost:8787

# ===== Payment Configuration =====
# Payment network identifier (base for mainnet, base-sepolia for testnet)
NETWORK=base
# Facilitator API base URL for x402 payment processing
FACILITATOR_URL=https://facilitator.daydreams.systems
# Address that receives x402 payments
PAY_TO=0x9e87e3c39fdba23549170de3bd30b5ad30269fc7
# Default price in USDC (automatically converted to base units)
# Examples: 0.01 = 1 cent, 0.1 = 10 cents, 1 = 1 dollar
DEFAULT_PRICE_USDC=0.01

# ===== Required Secrets =====
# Private key for signing payment requests (REQUIRED)
PRIVATE_KEY=

# Jatevo API key for AI model inference (REQUIRED)
JATEVO_API_KEY=

# ===== AI Model Configuration =====
# Jatevo API endpoint (default: https://inference.jatevo.id/v1/chat/completions)
JATEVO_API_ENDPOINT=https://inference.jatevo.id/v1/chat/completions
# Jatevo model name (default: deepseek-ai/DeepSeek-R1-0528)
# Available models: deepseek-ai/DeepSeek-R1-0528, zai-glm-4.6, glm-4-plus, etc.
# Note: DeepSeek-R1 is better at structured JSON output and reasoning
JATEVO_MODEL=deepseek-ai/DeepSeek-R1-0528

# ===== CoinGecko Configuration =====
# CoinGecko API base URL (optional, defaults to public API)
COINGECKO_BASE_URL=https://api.coingecko.com/api/v3

# ===== Advanced Options =====
# RPC endpoint for ERC-8004 trust interactions (optional)
RPC_URL=
# Auto-register agent identity at startup (optional)
REGISTER_IDENTITY=false
# Backward compatibility: price in base units (use DEFAULT_PRICE_USDC instead)
DEFAULT_PRICE=
